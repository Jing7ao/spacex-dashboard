name: Daily Briefing

on:
  schedule:
    - cron: '45 0 * * 1-5'   # 北京时间 8:45
    - cron: '30 7 * * 1-5'   # 北京时间 15:30
  workflow_dispatch:

jobs:
  brief:
    name: Run Briefing
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run Briefing
        run: |
          HOUR=$(TZ='Asia/Shanghai' date +%H)
          if [ "$HOUR" -lt 12 ]; then
            python morning_brief.py
          else
            python evening_brief.py
          fi
